# Notebooks

The active, user-facing notebooks live in `../examples/`.

Older exploratory notebooks were moved to `notebooks/stale/` during the public
release cleanup. They are kept for provenance, but they may reference archived
plots, removed intermediate data, or long-running search workflows.
